const loggerContainer = require('../index');

// 用console实现logger
loggerContainer.implement({
    getLogger: (category) => console
});

const logger = loggerContainer('console');

logger.info('log info');
logger.warn('log warn');
logger.error('log error');
