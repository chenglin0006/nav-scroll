var babel = require('gulp-babel');
var gulp = require('gulp');
gulp.task('babel', function() {
  gulp
    .src(['index.js'])
    .pipe(babel({
        "presets": ["es2015","stage-0"]
     }))
    .pipe(gulp.dest('./dist/'));
});
