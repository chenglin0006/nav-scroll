"use strict";
/**
 * @author yyy
 */

<% var deps = []; %>
<% for(dependencyKey in dependencies) { %>
   <% deps.push('"'+dependencies[dependencyKey]+'"') %>
<% } %>

DP.define([<%- deps.join(',') %>],function(DP, require, exports){
<% for(dependencyKey in dependencies) { %>var <%- dependencyKey %> = require('<%- dependencies[dependencyKey] %>');<% } %>
<%- contents %>
return <%- exports %>;
});