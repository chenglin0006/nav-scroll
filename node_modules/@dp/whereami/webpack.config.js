/**
 * Created by madlord on 16/1/14.
 */
var path = require('path');
var webpack = require('webpack');

var myPath = path.join(__dirname, '/demo', '/static');

var config = {
    entry: {
        "WhereAmI":'./index.js',
    },
    output: {
        path: myPath,
        filename: 'bundle.js',
        chunkFilename:'/chunk/[name].[chunkhash].js',
        publicPath: '/static',
        library: '[name]',
        libraryTarget: 'var'
    }
};

module.exports = config;
