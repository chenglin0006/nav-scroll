let Log = require("@dp/log");

module.exports = function(server,port){
	return function(){
		server.close();
		server.listen({
            port:port
        }, ()=> {
            Log.success(`create server:${port}`);
        })
	}
}