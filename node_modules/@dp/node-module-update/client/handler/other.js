let Log = require("@dp/log");
let router = require("../../util/router");
let CT = require("../../util/router");
const CLIENT_ROUTER_PATH = require("../../client/path");
module.exports = function(client,data){
	Log.error("clientError:"+data);
}